<h1>Books</h1>

<table>
	<div class="underbar">
		<tr>
			<th class="title">Title</th>
	   		<th class="opinion">Opinion</th>
	   	</tr>
	</div>

   	<%= @books.each do |book| %>
	<tr>

		<td> <%= attachment_image_tag @user, :profile_image, :fill, 20, 20, fallback: "no_image.jpg"%></td>
		<td><%= link_to book.title, book_path(@book_id) %></td>
	 	<td><%= book.body %></td>
   	</tr>
	<% end %>
</table>

